import React from "react";

export const metadata = {
  title: "VENDORS DETAILS PAGE",
  description: "VENDORS",
};

const VendorPageLayout = ({ children }) => {
  return <>{children}</>;
};

export default VendorPageLayout;
